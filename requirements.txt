fastapi
uvicorn
python-dotenv
google-generativeai
sentence-transformers
pinecone
requests

# A simplified and more robust set of dependencies for unstructured.io
unstructured

# Core dependencies for parsing specific file types with partition.auto
pypdf
python-docx
msg-parser
